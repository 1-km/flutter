import 'package:flutter/material.dart';

class CommunityFragment extends StatelessWidget {
  const CommunityFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: const Text('Community'), automaticallyImplyLeading: false),
    );
  }
}
